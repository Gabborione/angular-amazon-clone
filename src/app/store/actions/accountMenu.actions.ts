import { createAction, props } from "@ngrx/store";

export const openMenuAction = createAction(
    "[ACCOUNT_MENU] Open Menu"
);
export const closeMenuAction = createAction(
    '[ACCOUNT_MENU] Close Menu'
);
    